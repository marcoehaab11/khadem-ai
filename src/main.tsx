import React,{useMemo,useState} from 'react';import{createRoot}from'react-dom/client';import{Copy,Heart,Search}from'lucide-react';import{PROMPTS}from'./data/prompts';import'./styles.css';
function App(){const[q,setQ]=useState('');const[f,setF]=useState<string[]>([]);const[r,setR]=useState<any>(null);const items=useMemo(()=>PROMPTS.filter(p=>(p.title+p.summary+p.category+p.template).includes(q)),[q]);return <main><header><div className="wrap"><p className="eyebrow">مكتبة أدوات للخدمة المسيحية</p><h1>خادم <span>AI</span></h1><p className="lead">برومبتات عربية جاهزة للتعديل والنسخ تساعدك في التعليم، التخطيط، خدمة الأطفال والشباب وتدريب الخدام.</p><div className="search"><Search size={20}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث عن برومبت..."/></div><div className="stats"><b>{PROMPTS.length}</b><small>برومبت</small><b>7</b><small>تصنيفات</small><b>3</b><small>مصادر</small></div></div></header><section className="wrap"><h2>مكتبة البرومبتات</h2><div className="grid">{items.map(p=><article key={p.id}><div className="top"><span>{p.category}</span><button onClick={()=>setF(x=>x.includes(p.id)?x.filter(i=>i!==p.id):[...x,p.id])}><Heart size={19} fill={f.includes(p.id)?'currentColor':'none'}/></button></div><h3>{p.title}</h3><p>{p.summary}</p><textarea readOnly value={p.template}/><div className="actions"><button className="primary" onClick={()=>navigator.clipboard.writeText(p.template)}> <Copy size={17}/> نسخ البرومبت</button><button onClick={()=>setR(p)}>التفاصيل</button></div></article>)}</div></section>{r&&<div className="overlay" onClick={()=>setR(null)}><div className="modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setR(null)}>×</button><span>{r.category}</span><h2>{r.title}</h2><h4>متى تستخدمه؟</h4><p>{r.whenToUse}</p><h4>مثال</h4><p>{r.example}</p><h4>البرومبت</h4><textarea readOnly value={r.template}/></div></div>}<footer>هذه مكتبة تعليمية مستقلة. الصياغات أصلية ومستلهمة من موضوعات منشورة علنًا، ويجب مراجعة المخرجات قبل استخدامها في الخدمة.</footer></main>}createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);